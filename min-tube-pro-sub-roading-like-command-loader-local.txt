<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8" />
  <title>MIN-Tube-Pro Launch</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <style>
    :root {
      --bg: #02030a;
      --neon-cyan: #00f6ff;
      --neon-magenta: #ff00ff;
      --neon-purple: #a400ff;
      --neon-yellow: #ffe600;
      --text-main: #f5f7ff;
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    html, body {
      width: 100%;
      height: 100%;
      background: radial-gradient(circle at top, #10152b 0, #02030a 55%, #000000 100%);
      color: var(--text-main);
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      overflow: hidden;
    }

    body {
      display: flex;
      align-items: center;
      justify-content: center;
      perspective: 1600px;
    }

    .scene {
      position: relative;
      width: 260px;
      height: 260px;
      transform-style: preserve-3d;
      filter: drop-shadow(0 0 25px rgba(0, 255, 255, 0.45));
      animation: sceneFloat 6s ease-in-out infinite alternate;
    }

    @keyframes sceneFloat {
      0% {
        transform: translateY(0px) rotateX(18deg) rotateY(-22deg);
      }
      100% {
        transform: translateY(-18px) rotateX(26deg) rotateY(18deg);
      }
    }

    .cube {
      position: absolute;
      inset: 0;
      margin: auto;
      width: 220px;
      height: 220px;
      transform-style: preserve-3d;
      animation: cubeSpin 3.5s cubic-bezier(0.19, 1, 0.22, 1) forwards;
    }

    @keyframes cubeSpin {
      0% {
        transform: rotateX(-40deg) rotateY(-60deg) scale(0.2);
      }
      40% {
        transform: rotateX(20deg) rotateY(40deg) scale(1.05);
      }
      70% {
        transform: rotateX(35deg) rotateY(120deg) scale(1);
      }
      100% {
        transform: rotateX(25deg) rotateY(200deg) scale(1);
      }
    }

    .cube-face {
      position: absolute;
      width: 100%;
      height: 100%;
      border-radius: 18px;
      border: 2px solid rgba(255, 255, 255, 0.06);
      box-shadow:
        0 0 25px rgba(0, 246, 255, 0.35),
        0 0 55px rgba(255, 0, 255, 0.25),
        inset 0 0 35px rgba(0, 0, 0, 0.9);
      background:
        radial-gradient(circle at 20% 0%, rgba(0, 246, 255, 0.35), transparent 55%),
        radial-gradient(circle at 80% 100%, rgba(255, 0, 255, 0.35), transparent 55%),
        linear-gradient(135deg, rgba(10, 14, 40, 0.95), rgba(2, 3, 10, 0.98));
      backdrop-filter: blur(10px);
      -webkit-backdrop-filter: blur(10px);
    }

    .cube-face::before,
    .cube-face::after {
      content: "";
      position: absolute;
      inset: 10%;
      border-radius: 14px;
      border: 1px solid rgba(255, 255, 255, 0.08);
      box-shadow:
        0 0 18px rgba(0, 246, 255, 0.35),
        0 0 28px rgba(255, 0, 255, 0.25);
      opacity: 0.9;
    }

    .cube-face::after {
      inset: 22%;
      border-radius: 10px;
      border-color: rgba(255, 255, 255, 0.12);
      box-shadow:
        0 0 18px rgba(255, 230, 0, 0.35),
        0 0 30px rgba(0, 246, 255, 0.25);
      opacity: 0.7;
    }

    .cube-face.front  { transform: translateZ(110px); }
    .cube-face.back   { transform: rotateY(180deg) translateZ(110px); }
    .cube-face.right  { transform: rotateY(90deg) translateZ(110px); }
    .cube-face.left   { transform: rotateY(-90deg) translateZ(110px); }
    .cube-face.top    { transform: rotateX(90deg) translateZ(110px); }
    .cube-face.bottom { transform: rotateX(-90deg) translateZ(110px); }

    .cube-grid {
      position: absolute;
      inset: 16%;
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      grid-template-rows: repeat(4, 1fr);
      gap: 6px;
      opacity: 0.9;
    }

    .cube-cell {
      position: relative;
      border-radius: 6px;
      background: radial-gradient(circle at 30% 0%, rgba(0, 246, 255, 0.7), rgba(0, 0, 0, 0.9));
      box-shadow:
        0 0 10px rgba(0, 246, 255, 0.6),
        0 0 18px rgba(255, 0, 255, 0.45);
      overflow: hidden;
    }

    .cube-cell::before {
      content: "";
      position: absolute;
      inset: -40%;
      background:
        conic-gradient(
          from 0deg,
          rgba(0, 246, 255, 0.0),
          rgba(0, 246, 255, 0.9),
          rgba(255, 0, 255, 0.9),
          rgba(255, 230, 0, 0.9),
          rgba(0, 246, 255, 0.0)
        );
      mix-blend-mode: screen;
      animation: cellSweep 3.5s linear infinite;
    }

    @keyframes cellSweep {
      0%   { transform: rotate(0deg); }
      100% { transform: rotate(360deg); }
    }

    .cube-cell:nth-child(odd)::before {
      animation-duration: 4.2s;
    }
    .cube-cell:nth-child(3n)::before {
      animation-duration: 3.1s;
    }

    .cube-core {
      position: absolute;
      inset: 32%;
      border-radius: 18px;
      background:
        radial-gradient(circle at 50% 0%, rgba(255, 255, 255, 0.9), transparent 60%),
        radial-gradient(circle at 0% 100%, rgba(0, 246, 255, 0.9), transparent 60%),
        radial-gradient(circle at 100% 100%, rgba(255, 0, 255, 0.9), transparent 60%);
      box-shadow:
        0 0 40px rgba(255, 255, 255, 0.9),
        0 0 80px rgba(0, 246, 255, 0.9),
        0 0 120px rgba(255, 0, 255, 0.9);
      opacity: 0;
      animation: corePulse 3.5s ease-out forwards;
    }

    @keyframes corePulse {
      0% {
        opacity: 0;
        transform: scale(0.4);
      }
      40% {
        opacity: 1;
        transform: scale(1.05);
      }
      70% {
        opacity: 0.95;
        transform: scale(0.95);
      }
      100% {
        opacity: 0.0;
        transform: scale(0.6);
      }
    }

    .floor {
      position: absolute;
      width: 520px;
      height: 520px;
      border-radius: 50%;
      background:
        radial-gradient(circle, rgba(0, 246, 255, 0.4), transparent 55%),
        radial-gradient(circle, rgba(255, 0, 255, 0.3), transparent 70%);
      opacity: 0.55;
      transform: rotateX(90deg) translateZ(-180px);
      filter: blur(2px);
    }

    .floor-grid {
      position: absolute;
      inset: 0;
      background-image:
        linear-gradient(rgba(0, 246, 255, 0.25) 1px, transparent 1px),
        linear-gradient(90deg, rgba(0, 246, 255, 0.25) 1px, transparent 1px);
      background-size: 26px 26px;
      mask-image: radial-gradient(circle, black 0, transparent 70%);
      opacity: 0.7;
      animation: floorGlow 4.5s ease-in-out infinite alternate;
    }

    @keyframes floorGlow {
      0% {
        opacity: 0.4;
        transform: scale(0.9);
      }
      100% {
        opacity: 0.9;
        transform: scale(1.05);
      }
    }

    .logo {
      position: absolute;
      inset: auto 0 40px 0;
      margin: auto;
      width: 100%;
      text-align: center;
      letter-spacing: 0.18em;
      font-size: 0.78rem;
      text-transform: uppercase;
      color: rgba(200, 210, 255, 0.7);
      text-shadow:
        0 0 10px rgba(0, 246, 255, 0.6),
        0 0 18px rgba(255, 0, 255, 0.4);
      opacity: 0;
      animation: logoFade 3.5s ease-out forwards;
    }

    @keyframes logoFade {
      0% {
        opacity: 0;
        transform: translateY(10px);
      }
      40% {
        opacity: 1;
        transform: translateY(0);
      }
      80% {
        opacity: 0.9;
      }
      100% {
        opacity: 0;
        transform: translateY(6px);
      }
    }

    .logo span {
      display: inline-block;
      padding: 0.25em 0.7em;
      border-radius: 999px;
      border: 1px solid rgba(0, 246, 255, 0.6);
      background: radial-gradient(circle at 0 0, rgba(0, 246, 255, 0.4), transparent 55%);
      box-shadow:
        0 0 16px rgba(0, 246, 255, 0.7),
        0 0 26px rgba(255, 0, 255, 0.5);
    }

    .logo span strong {
      font-weight: 700;
      color: #ffffff;
    }

    .dive-message {
      position: absolute;
      inset: 0;
      display: flex;
      align-items: center;
      justify-content: center;
      flex-direction: column;
      opacity: 0;
      pointer-events: none;
      text-align: center;
      color: var(--text-main);
      text-shadow:
        0 0 18px rgba(0, 246, 255, 0.9),
        0 0 32px rgba(255, 0, 255, 0.8);
    }

    .dive-title {
      font-size: clamp(2.4rem, 4vw, 3.2rem);
      letter-spacing: 0.18em;
      text-transform: uppercase;
      margin-bottom: 0.6rem;
      background: linear-gradient(120deg, #00f6ff, #ffe600, #ff00ff, #00f6ff);
      -webkit-background-clip: text;
      background-clip: text;
      color: transparent;
      position: relative;
    }

    .dive-title::after {
      content: "";
      position: absolute;
      inset: -8px -18px;
      border-radius: 999px;
      border: 1px solid rgba(0, 246, 255, 0.7);
      box-shadow:
        0 0 20px rgba(0, 246, 255, 0.9),
        0 0 40px rgba(255, 0, 255, 0.9),
        0 0 70px rgba(255, 230, 0, 0.9);
      opacity: 0.9;
      filter: blur(0.3px);
    }

    .dive-sub {
      font-size: 0.9rem;
      letter-spacing: 0.22em;
      text-transform: uppercase;
      color: rgba(210, 220, 255, 0.85);
      margin-top: 0.4rem;
    }

    .dive-glow {
      position: absolute;
      width: 420px;
      height: 420px;
      border-radius: 50%;
      background:
        radial-gradient(circle, rgba(0, 246, 255, 0.55), transparent 60%),
        radial-gradient(circle, rgba(255, 0, 255, 0.45), transparent 70%);
      opacity: 0.55;
      filter: blur(4px);
      z-index: -1;
    }

    .dive-message.show {
      animation: diveIn 1.5s cubic-bezier(0.19, 1, 0.22, 1) forwards;
    }

    @keyframes diveIn {
      0% {
        opacity: 0;
        transform: scale(0.8) translateY(20px);
      }
      40% {
        opacity: 1;
        transform: scale(1.02) translateY(0);
      }
      100% {
        opacity: 1;
        transform: scale(1) translateY(0);
      }
    }

    .dive-message.hide {
      animation: diveOut 0.6s ease-in forwards;
    }

    @keyframes diveOut {
      0% {
        opacity: 1;
        transform: scale(1);
      }
      100% {
        opacity: 0;
        transform: scale(1.05) translateY(-10px);
      }
    }

    .main-ui {
      position: absolute;
      inset: 0;
      display: flex;
      align-items: center;
      justify-content: center;
      opacity: 0;
      pointer-events: none;
      color: var(--text-main);
    }

    .main-ui.show {
      animation: uiFadeIn 0.7s ease-out forwards;
    }

    @keyframes uiFadeIn {
      0% {
        opacity: 0;
        transform: translateY(10px);
      }
      100% {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .main-logo {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 0.4rem;
    }

    .main-logo-top {
      font-size: 0.8rem;
      letter-spacing: 0.28em;
      text-transform: uppercase;
      color: rgba(180, 195, 255, 0.8);
    }

    .main-logo-mid {
      font-size: clamp(2.2rem, 4vw, 3rem);
      font-weight: 800;
      letter-spacing: 0.18em;
      text-transform: uppercase;
      background: linear-gradient(120deg, #00f6ff, #ffe600, #ff00ff, #00f6ff);
      -webkit-background-clip: text;
      background-clip: text;
      color: transparent;
      text-shadow:
        0 0 18px rgba(0, 246, 255, 0.9),
        0 0 32px rgba(255, 0, 255, 0.8);
    }

    .main-logo-bottom {
      font-size: 0.8rem;
      letter-spacing: 0.22em;
      text-transform: uppercase;
      color: rgba(160, 180, 240, 0.9);
    }

    @media (max-width: 480px) {
      .scene {
        width: 220px;
        height: 220px;
      }
      .cube {
        width: 190px;
        height: 190px;
      }
      .floor {
        width: 420px;
        height: 420px;
      }
      .dive-title::after {
        inset: -6px -14px;
      }
    }
  </style>
</head>
<body>
  <div class="scene" id="scene">
    <div class="cube" id="cube">
      <div class="cube-face front">
        <div class="cube-grid">
          <!-- 4x4 = 16 cells -->
          <div class="cube-cell"></div><div class="cube-cell"></div><div class="cube-cell"></div><div class="cube-cell"></div>
          <div class="cube-cell"></div><div class="cube-cell"></div><div class="cube-cell"></div><div class="cube-cell"></div>
          <div class="cube-cell"></div><div class="cube-cell"></div><div class="cube-cell"></div><div class="cube-cell"></div>
          <div class="cube-cell"></div><div class="cube-cell"></div><div class="cube-cell"></div><div class="cube-cell"></div>
        </div>
        <div class="cube-core"></div>
      </div>
      <div class="cube-face back"></div>
      <div class="cube-face right"></div>
      <div class="cube-face left"></div>
      <div class="cube-face top"></div>
      <div class="cube-face bottom"></div>
    </div>

    <div class="floor">
      <div class="floor-grid"></div>
    </div>

    <div class="logo">
      <span><strong>MIN</strong>-Tube-Pro</span>
    </div>
  </div>

  <div class="dive-message" id="diveMessage">
    <div class="dive-glow"></div>
    <div class="dive-title">Dive to MIN-Tube-Pro</div>
    <div class="dive-sub">The powerful yt-get engine</div>
  </div>

  <div class="main-ui" id="mainUI">
    <div class="main-logo">
      <div class="main-logo-top">Welcome to</div>
      <div class="main-logo-mid">MIN-Tube-Pro</div>
      <div class="main-logo-bottom">Streaming interface</div>
    </div>
  </div>

  <script>
    (function () {
      const INTRO_DURATION = 3500; 
      const DIVE_DURATION = 1500;  

      const scene = document.getElementById("scene");
      const cube = document.getElementById("cube");
      const diveMessage = document.getElementById("diveMessage");
      const mainUI = document.getElementById("mainUI");

      function showDiveMessage() {
        diveMessage.classList.add("show");
      }

      function hideIntroAndShowUI() {
        diveMessage.classList.remove("show");
        diveMessage.classList.add("hide");

        scene.style.transition = "opacity 0.7s ease-in, transform 0.7s ease-in";
        scene.style.opacity = "0";
        scene.style.transform = "scale(0.96) translateY(-10px)";

        setTimeout(() => {
          mainUI.classList.add("show");
        }, 250);
      }

      setTimeout(showDiveMessage, INTRO_DURATION);
      setTimeout(hideIntroAndShowUI, INTRO_DURATION + DIVE_DURATION);

      window.addEventListener("resize", () => {
      });
    })();
  </script>
</body>
</html>
