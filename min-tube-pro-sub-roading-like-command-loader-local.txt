<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8" />
  <title>MIN-Tube-Pro Launch</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <style>
    :root {
      --bg: #02030a;
      --neon-cyan: #27f3ff;
      --neon-magenta: #ff3bff;
      --neon-green: #4dff7a;
      --neon-yellow: #ffe45e;
      --neon-red: #ff3366;
      --text-main: #f5f7ff;
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    html, body {
      width: 100%;
      height: 100%;
      background: radial-gradient(circle at top, #05081a 0, #02030a 45%, #000 100%);
      color: var(--text-main);
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      overflow: hidden;
    }

    body {
      display: flex;
      align-items: center;
      justify-content: center;
      perspective: 1600px;
    }

    .scene {
      position: relative;
      width: 100vw;
      height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      overflow: hidden;
    }

    .bg-glow {
      position: absolute;
      inset: -20%;
      background:
        radial-gradient(circle at 20% 10%, rgba(39,243,255,0.18) 0, transparent 55%),
        radial-gradient(circle at 80% 20%, rgba(255,59,255,0.18) 0, transparent 55%),
        radial-gradient(circle at 10% 80%, rgba(77,255,122,0.16) 0, transparent 55%),
        radial-gradient(circle at 90% 85%, rgba(255,228,94,0.16) 0, transparent 55%);
      filter: blur(4px);
      opacity: 0.9;
      pointer-events: none;
      mix-blend-mode: screen;
    }

    .logo-wrapper {
      position: relative;
      width: min(520px, 80vw);
      height: min(260px, 40vh);
      transform-style: preserve-3d;
      animation: logoFloat 6s ease-in-out infinite alternate;
    }

    @keyframes logoFloat {
      0% {
        transform: rotateX(18deg) rotateY(-22deg) translateY(0);
      }
      50% {
        transform: rotateX(10deg) rotateY(18deg) translateY(-6px);
      }
      100% {
        transform: rotateX(16deg) rotateY(-10deg) translateY(4px);
      }
    }

    .panel {
      position: absolute;
      inset: 0;
      border-radius: 26px;
      background: linear-gradient(135deg, #050814 0%, #050b1f 40%, #02030a 100%);
      box-shadow:
        0 0 0 1px rgba(255,255,255,0.04),
        0 26px 80px rgba(0,0,0,0.9),
        0 0 40px rgba(39,243,255,0.25),
        0 0 80px rgba(255,59,255,0.18);
      overflow: hidden;
      transform-style: preserve-3d;
    }

    .panel::before {
      content: "";
      position: absolute;
      inset: -40%;
      background:
        conic-gradient(from 120deg,
          rgba(39,243,255,0.0),
          rgba(39,243,255,0.7),
          rgba(255,59,255,0.0),
          rgba(255,228,94,0.7),
          rgba(77,255,122,0.0)
        );
      opacity: 0.22;
      mix-blend-mode: screen;
      animation: panelGlow 10s linear infinite;
    }

    @keyframes panelGlow {
      0% { transform: rotate(0deg) translate3d(0,0,0); }
      100% { transform: rotate(360deg) translate3d(0,0,0); }
    }

    .boot-logo {
      position: absolute;
      inset: 0;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      gap: 26px;
      transform: translateZ(40px);
      animation: bootIntro 5s ease-out forwards;
    }

    @keyframes bootIntro {
      0% {
        opacity: 0;
        transform: translateZ(120px) scale(0.9);
        filter: blur(6px);
      }
      12% {
        opacity: 1;
        transform: translateZ(40px) scale(1.02);
        filter: blur(0);
      }
      40% {
        transform: translateZ(60px) scale(1);
      }
      70% {
        transform: translateZ(40px) scale(1);
      }
      100% {
        opacity: 0;
        transform: translateZ(0px) scale(0.96);
        filter: blur(4px);
      }
    }

    .boot-title {
      font-size: clamp(2.6rem, 4vw, 3.4rem);
      letter-spacing: 0.12em;
      text-transform: uppercase;
      color: #e9f7ff;
      text-align: center;
      position: relative;
      padding: 0.2em 0.6em;
      text-shadow:
        0 0 12px rgba(39,243,255,0.9),
        0 0 26px rgba(255,59,255,0.8),
        0 0 50px rgba(0,0,0,1);
    }

    .boot-title span.min {
      color: var(--neon-cyan);
    }
    .boot-title span.tube {
      color: var(--neon-magenta);
    }
    .boot-title span.pro {
      color: var(--neon-green);
    }

    .boot-title::before,
    .boot-title::after {
      content: "";
      position: absolute;
      left: 50%;
      width: 120%;
      height: 2px;
      border-radius: 999px;
      background: linear-gradient(90deg,
        transparent,
        rgba(39,243,255,0.9),
        rgba(255,59,255,0.9),
        rgba(77,255,122,0.9),
        transparent
      );
      transform: translateX(-50%);
      box-shadow:
        0 0 12px rgba(39,243,255,0.9),
        0 0 18px rgba(255,59,255,0.9);
    }

    .boot-title::before {
      top: -0.4em;
    }
    .boot-title::after {
      bottom: -0.4em;
    }

    .boot-sub {
      font-size: 0.9rem;
      letter-spacing: 0.18em;
      text-transform: uppercase;
      color: rgba(230,240,255,0.8);
      text-align: center;
      opacity: 0.9;
    }

    .boot-progress {
      position: relative;
      width: 60%;
      max-width: 320px;
      height: 4px;
      border-radius: 999px;
      background: rgba(255,255,255,0.06);
      overflow: hidden;
      box-shadow:
        0 0 12px rgba(39,243,255,0.5),
        0 0 18px rgba(255,59,255,0.4);
    }

    .boot-progress-bar {
      position: absolute;
      inset: 0;
      background: linear-gradient(90deg,
        rgba(39,243,255,0.0),
        rgba(39,243,255,1),
        rgba(255,59,255,1),
        rgba(77,255,122,1),
        rgba(255,228,94,1),
        rgba(255,51,102,1),
        rgba(255,51,102,0.0)
      );
      transform-origin: left center;
      animation: bootProgress 5s cubic-bezier(0.16, 0.9, 0.25, 1) forwards;
    }

    @keyframes bootProgress {
      0% { transform: scaleX(0); }
      20% { transform: scaleX(0.25); }
      45% { transform: scaleX(0.55); }
      70% { transform: scaleX(0.82); }
      100% { transform: scaleX(1); }
    }

    .boot-hint {
      margin-top: 6px;
      font-size: 0.7rem;
      letter-spacing: 0.16em;
      text-transform: uppercase;
      color: rgba(200,215,255,0.6);
    }

    .dive-screen {
      position: absolute;
      inset: 0;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      gap: 24px;
      transform: translateZ(50px);
      opacity: 0;
      pointer-events: none;
    }

    .dive-screen.active {
      animation: diveIn 1.4s cubic-bezier(0.16, 0.9, 0.25, 1) forwards;
      pointer-events: auto;
    }

    @keyframes diveIn {
      0% {
        opacity: 0;
        transform: translateZ(0px) translateY(18px) scale(0.96);
        filter: blur(6px);
      }
      60% {
        opacity: 1;
        transform: translateZ(60px) translateY(0) scale(1.02);
        filter: blur(0);
      }
      100% {
        opacity: 1;
        transform: translateZ(50px) translateY(0) scale(1);
      }
    }

    .dive-label {
      font-size: 0.9rem;
      letter-spacing: 0.28em;
      text-transform: uppercase;
      color: rgba(210,225,255,0.7);
    }

    .dive-title {
      font-size: clamp(2.4rem, 4vw, 3.2rem);
      text-transform: uppercase;
      letter-spacing: 0.18em;
      text-align: center;
      position: relative;
      padding: 0.4em 0.8em;
      color: #fdfdff;
      text-shadow:
        0 0 14px rgba(39,243,255,1),
        0 0 26px rgba(255,59,255,1),
        0 0 40px rgba(77,255,122,0.9),
        0 0 80px rgba(0,0,0,1);
    }

    .dive-title span.dive {
      color: var(--neon-cyan);
    }
    .dive-title span.to {
      color: var(--neon-yellow);
    }
    .dive-title span.min {
      color: var(--neon-cyan);
    }
    .dive-title span.tube {
      color: var(--neon-magenta);
    }
    .dive-title span.pro {
      color: var(--neon-green);
    }

    .dive-title::before {
      content: "";
      position: absolute;
      inset: 0;
      border-radius: 999px;
      border: 1px solid rgba(255,255,255,0.12);
      box-shadow:
        0 0 18px rgba(39,243,255,0.9),
        0 0 26px rgba(255,59,255,0.9),
        0 0 40px rgba(77,255,122,0.9);
      opacity: 0.9;
    }

    .dive-title::after {
      content: "";
      position: absolute;
      inset: 18%;
      border-radius: 999px;
      border: 1px dashed rgba(255,255,255,0.18);
      opacity: 0.7;
      filter: blur(0.4px);
    }

    .dive-pulse-ring {
      position: absolute;
      width: 220%;
      height: 220%;
      border-radius: 50%;
      border: 2px solid rgba(39,243,255,0.0);
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      animation: divePulse 3.2s ease-out infinite;
      pointer-events: none;
    }

    @keyframes divePulse {
      0% {
        transform: translate(-50%, -50%) scale(0.4);
        border-color: rgba(39,243,255,0.9);
        opacity: 0.9;
      }
      60% {
        border-color: rgba(255,59,255,0.4);
        opacity: 0.4;
      }
      100% {
        transform: translate(-50%, -50%) scale(1.1);
        border-color: rgba(39,243,255,0.0);
        opacity: 0;
      }
    }

    .dive-cta {
      margin-top: 10px;
      padding: 0.7em 1.8em;
      border-radius: 999px;
      border: 1px solid rgba(255,255,255,0.18);
      background: radial-gradient(circle at top, rgba(39,243,255,0.3), rgba(2,3,10,0.9));
      color: #fdfdff;
      font-size: 0.9rem;
      letter-spacing: 0.18em;
      text-transform: uppercase;
      display: inline-flex;
      align-items: center;
      gap: 0.6em;
      cursor: pointer;
      box-shadow:
        0 0 16px rgba(39,243,255,0.8),
        0 0 26px rgba(255,59,255,0.7);
      position: relative;
      overflow: hidden;
    }

    .dive-cta::before {
      content: "";
      position: absolute;
      inset: 0;
      background: linear-gradient(120deg,
        rgba(255,255,255,0.0) 0%,
        rgba(255,255,255,0.5) 40%,
        rgba(255,255,255,0.0) 100%
      );
      transform: translateX(-120%);
      animation: ctaShine 3.4s ease-in-out infinite;
    }

    @keyframes ctaShine {
      0% { transform: translateX(-120%); }
      40% { transform: translateX(120%); }
      100% { transform: translateX(120%); }
    }

    .dive-cta-icon {
      width: 1.1em;
      height: 1.1em;
      border-radius: 50%;
      border: 1px solid rgba(255,255,255,0.7);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 0.7rem;
      box-shadow:
        0 0 10px rgba(39,243,255,0.9),
        0 0 18px rgba(255,59,255,0.9);
    }

    .dive-meta {
      font-size: 0.7rem;
      letter-spacing: 0.16em;
      text-transform: uppercase;
      color: rgba(190,205,240,0.7);
    }

    @media (max-width: 600px) {
      .logo-wrapper {
        height: min(220px, 42vh);
      }
      .boot-sub {
        font-size: 0.8rem;
      }
      .boot-progress {
        width: 70%;
      }
      .dive-label {
        font-size: 0.8rem;
      }
      .dive-meta {
        font-size: 0.65rem;
      }
    }
  </style>
</head>
<body>
  <div class="scene">
    <div class="bg-glow"></div>

    <div class="logo-wrapper">
      <div class="panel">
        <div class="boot-logo" id="bootLogo">
          <div class="boot-title">
            <span class="min">MIN</span>-<span class="tube">Tube</span>-<span class="pro">Pro</span>
          </div>
          <div class="boot-sub">Ultra Neon Streaming Interface</div>
          <div class="boot-progress">
            <div class="boot-progress-bar"></div>
          </div>
          <div class="boot-hint">Initializing immersive experience...</div>
        </div>

        <div class="dive-screen" id="diveScreen">
          <div class="dive-label">Welcome</div>
          <div class="dive-title">
            <span class="dive">Dive</span> <span class="to">to</span>
            <span class="min">MIN</span>-<span class="tube">Tube</span>-<span class="pro">Pro</span>
            <div class="dive-pulse-ring"></div>
          </div>
          <button class="dive-cta" onclick="onDiveClick()">
            <span>Enter the Neon Space</span>
            <span class="dive-cta-icon">▶</span>
          </button>
          <div class="dive-meta">Perfectly tuned for your next deep watch</div>
        </div>
      </div>
    </div>
  </div>

  <script>
    window.addEventListener("load", () => {
      const boot = document.getElementById("bootLogo");
      const dive = document.getElementById("diveScreen");

      setTimeout(() => {
        if (boot) boot.style.display = "none";
        if (dive) dive.classList.add("active");
      }, 3000); 
    });

    function onDiveClick() {
      console.log("Dive to MIN-Tube-Pro!");
    }
  </script>
</body>
</html>
