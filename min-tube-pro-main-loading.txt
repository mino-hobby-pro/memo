<!DOCTYPE html> <html lang="ja"> <head> <meta charset="UTF-8"> <title>MIN-Tube-Pro | The Eternal Flux</title> <style> :root { --bg: #03040a; --gold: #c5a059; --cyan: #00e5ff; } body, html { margin: 0; padding: 0; width: 100%; height: 100%; background: var(--bg); overflow: hidden; font-family: 'Cinzel', serif; } #app { position: absolute; inset: 0; z-index: 1; cursor: none; } .interface { position: absolute; inset: 0; z-index: 10; display: flex; align-items: center; justify-content: center; pointer-events: none; mix-blend-mode: exclusion; } .brand-logo { font-size: clamp(1.5rem, 5vw, 3.5rem); font-weight: 100; color: #fff; letter-spacing: 2rem; text-transform: uppercase; opacity: 0; transform: translateY(30px) scale(1.1); transition: all 2.5s cubic-bezier(0.075, 0.82, 0.165, 1); } .brand-logo.visible { opacity: 0.9; transform: translateY(0) scale(1); filter: blur(0); } .scanline { position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: linear-gradient(to bottom, rgba(18, 16, 16, 0) 50%, rgba(0, 0, 0, 0.25) 50%), linear-gradient(90deg, rgba(255, 0, 0, 0.06), rgba(0, 255, 0, 0.02), rgba(0, 0, 255, 0.06)); background-size: 100% 4px, 3px 100%; pointer-events: none; z-index: 100; } </style> <link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400;900&display=swap" rel="stylesheet"> </head> <body> <div id="app"></div> <div class="scanline"></div> <div class="interface"> <div id="logo" class="brand-logo">MIN-Tube-Pro</div> </div> <script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js"></script> <script src="https://cdnjs.cloudflare.com/ajax/libs/tween.js/18.6.4/tween.umd.js"></script> <script id="vertexShader" type="x-shader/x-vertex"> uniform float uTime; uniform float uInflate; varying vec2 vUv; varying vec3 vNormal; varying vec3 vViewPosition; vec3 mod289(vec3 x) { return x - floor(x * (1.0 / 289.0)) * 289.0; } vec4 mod289(vec4 x) { return x - floor(x * (1.0 / 289.0)) * 289.0; } vec4 permute(vec4 x) { return mod289(((x*34.0)+1.0)*x); } vec4 taylorInvSqrt(vec4 r) { return 1.79284291400159 - 0.85373472095314 * r; } float snoise(vec3 v) { const vec2  C = vec2(1.0/6.0, 1.0/3.0); const vec4  D = vec4(0.0, 0.5, 1.0, 2.0); vec3 i  = floor(v + dot(v, C.yyy)); vec3 x0 = v - i + dot(i, C.xxx); vec3 g = step(x0.yzx, x0.xyz); vec3 l = 1.0 - g; vec3 i1 = min( g.xyz, l.zxy ); vec3 i2 = max( g.xyz, l.zxy ); vec3 x1 = x0 - i1 + C.xxx; vec3 x2 = x0 - i2 + C.yyy; vec3 x3 = x0 - D.yyy; i = mod289(i); vec4 p = permute( permute( permute( i.z + vec4(0.0, i1.z, i2.z, 1.0 )) + i.y + vec4(0.0, i1.y, i2.y, 1.0 )) + i.x + vec4(0.0, i1.x, i2.x, 1.0 )); float n_ = 0.142857142857; vec3  ns = n_ * D.wyz - D.xzx; vec4 j = p - 49.0 * floor(p * ns.z * ns.z); vec4 x_ = floor(j * ns.z); vec4 y_ = floor(j - 7.0 * x_ ); vec4 x = x_ *ns.x + ns.yyyy; vec4 y = y_ *ns.x + ns.yyyy; vec4 h = 1.0 - abs(x) - abs(y); vec4 b0 = vec4( x.xy, y.xy ); vec4 b1 = vec4( x.zw, y.zw ); vec4 s0 = floor(b0)*2.0 + 1.0; vec4 s1 = floor(b1)*2.0 + 1.0; vec4 sh = -step(h, vec4(0.0)); vec4 a0 = b0.xzyw + s0.xzyw*sh.xxyy ; vec4 a1 = b1.xzyw + s1.xzyw*sh.zzww ; vec3 p0 = vec3(a0.xy,h.x); vec3 p1 = vec3(a0.zw,h.y); vec3 p2 = vec3(a1.xy,h.z); vec3 p3 = vec3(a1.zw,h.w); vec4 norm = taylorInvSqrt(vec4(dot(p0,p0), dot(p1,p1), dot(p2, p2), dot(p3,p3))); p0 *= norm.x; p1 *= norm.y; p2 *= norm.z; p3 *= norm.w; vec4 m = max(0.6 - vec4(dot(x0,x0), dot(x1,x1), dot(x2,x2), dot(x3,x3)), 0.0); m = m * m; return 42.0 * dot( m*m, vec4( dot(p0,x0), dot(p1,x1), dot(p2,x2), dot(p3,x3) ) ); } void main() { vUv = uv; vNormal = normalize(normalMatrix * normal); float noise = snoise(vec3(position.xy * 0.5, uTime * 0.6)); vec3 newPosition = position + normal * noise * uInflate; vec4 mvPosition = modelViewMatrix * vec4(newPosition, 1.0); vViewPosition = -mvPosition.xyz; gl_Position = projectionMatrix * mvPosition; } </script> <script id="fragmentShader" type="x-shader/x-fragment"> uniform float uTime; uniform vec3 uColor1; uniform vec3 uColor2; varying vec2 vUv; varying vec3 vNormal; varying vec3 vViewPosition; void main() { vec3 normal = normalize(vNormal); vec3 viewDir = normalize(vViewPosition); float fresnel = pow(1.0 - dot(normal, viewDir), 3.0); vec3 baseColor = mix(uColor1, uColor2, sin(vUv.x * 10.0 + uTime) * 0.5 + 0.5); vec3 finalColor = baseColor + fresnel * 0.8; gl_FragColor = vec4(finalColor, 0.85); } </script> <script> class UltraEngine { constructor() { this.scene = new THREE.Scene(); this.camera = new THREE.PerspectiveCamera(55, window.innerWidth / window.innerHeight, 0.01, 2000); this.renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true }); this.clock = new THREE.Clock(); this.fragments = []; this.isSublimated = false; this.initRenderer(); this.createLights(); this.createFluidCube(); this.createEnvironment(); this.startSequence(); this.animate(); } initRenderer() { this.renderer.setSize(window.innerWidth, window.innerHeight); this.renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2)); this.renderer.toneMapping = THREE.ACESFilmicToneMapping; document.getElementById('app').appendChild(this.renderer.domElement); this.camera.position.set(0, 0, 5); } createLights() { const light1 = new THREE.SpotLight(0xffffff, 2); light1.position.set(10, 10, 10); this.scene.add(light1); const light2 = new THREE.RectAreaLight(0x00e5ff, 5, 10, 10); light2.position.set(-5, -5, 5); this.scene.add(light2); } createFluidCube() { this.cubeMaterial = new THREE.ShaderMaterial({ vertexShader: document.getElementById('vertexShader').textContent, fragmentShader: document.getElementById('fragmentShader').textContent, uniforms: { uTime: { value: 0 }, uInflate: { value: 0.2 }, uColor1: { value: new THREE.Color(0x050810) }, uColor2: { value: new THREE.Color(0xc5a059) } }, transparent: true, side: THREE.DoubleSide }); this.cubeGeo = new THREE.BoxGeometry(1.5, 1.5, 1.5, 64, 64, 64); this.fluidCube = new THREE.Mesh(this.cubeGeo, this.cubeMaterial); this.scene.add(this.fluidCube); } createEnvironment() { const floorGeo = new THREE.PlaneGeometry(100, 100, 128, 128); const floorMat = new THREE.MeshStandardMaterial({ color: 0x010205, metalness: 1, roughness: 0.1 }); this.floor = new THREE.Mesh(floorGeo, floorMat); this.floor.rotation.x = -Math.PI / 2; this.floor.position.y = -5; this.scene.add(this.floor); } startSequence() { const T = window.TWEEN; new T.Tween(this.cubeMaterial.uniforms.uInflate) .to({ value: 0.8 }, 1500) .easing(T.Easing.Quadratic.InOut) .start(); setTimeout(() => { new T.Tween(this.fluidCube.rotation) .to({ x: Math.PI * 2, y: Math.PI }, 1500) .easing(T.Easing.Quintic.InOut) .start(); new T.Tween(this.camera.position) .to({ z: 12, y: 5 }, 1500) .easing(T.Easing.Cubic.InOut) .start(); }, 1500); setTimeout(() => { this.sublimationEffect(); }, 3000); setTimeout(() => { document.getElementById('logo').classList.add('visible'); }, 4000); } sublimationEffect() { this.isSublimated = true; this.fluidCube.visible = false; const partGeo = new THREE.SphereGeometry(0.02, 8, 8); for(let i=0; i<800; i++) { const partMat = new THREE.MeshBasicMaterial({ color: 0xc5a059, transparent: true }); const part = new THREE.Mesh(partGeo, partMat); part.position.copy(this.fluidCube.position); this.scene.add(part); const angle = Math.random() * Math.PI * 2; const force = Math.random() * 20 + 5; new window.TWEEN.Tween(part.position) .to({ x: Math.cos(angle) * force, y: Math.random() * 20, z: Math.sin(angle) * force }, 2000) .easing(window.TWEEN.Easing.Exponential.Out) .start(); new window.TWEEN.Tween(partMat) .to({ opacity: 0 }, 1500) .delay(500) .start(); } } animate() { requestAnimationFrame(() => this.animate()); const delta = this.clock.getElapsedTime(); window.TWEEN.update(); if (this.cubeMaterial) { this.cubeMaterial.uniforms.uTime.value = delta; } if (!this.isSublimated) { this.fluidCube.rotation.y += 0.005; this.fluidCube.rotation.z += 0.003; } this.renderer.render(this.scene, this.camera); } } window.onload = () => { new UltraEngine(); }; function complexMathKernel() { let storage = []; for(let i=0; i<500; i++) { storage.push(Math.tan(Math.atan(Math.sqrt(i)))); } return storage; } complexMathKernel(); </script> </body> </html>
